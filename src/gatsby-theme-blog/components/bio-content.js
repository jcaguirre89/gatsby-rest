import React, { Fragment } from "react"
import { Styled } from "theme-ui"
import GatsbyLink from "gatsby-link";

/**
 * Change the content to add your own bio
 */

export default () => (
  <Fragment>
    <GatsbyLink to='/donations'>
      Ir al sitio de donaciones según ley de lobby
    </GatsbyLink>
  </Fragment>
)
